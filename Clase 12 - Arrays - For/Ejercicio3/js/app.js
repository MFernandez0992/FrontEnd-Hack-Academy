var productos = ['Monitor 27\"', 'Macbook Pro', 'iPhone'];

function mostrarArray(productos){
    for (let i = 0; i < productos.length; i++) {
        console.log(productos[i]);
    }
}

mostrarArray(productos);